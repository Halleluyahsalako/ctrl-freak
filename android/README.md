# android/

Kotlin + Jetpack Compose app.

Not scaffolded yet — generating a real Gradle/Compose project needs Android Studio's project wizard rather than hand-written build files, so this happens at the start of Phase 1 (see `../ARCHITECTURE.md §4`) when we're ready to write actual UI code.
