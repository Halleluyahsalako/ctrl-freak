# android/

Kotlin + Jetpack Compose app.

Not scaffolded yet — generating a real Gradle/Compose project needs Android Studio's project wizard rather than hand-written build files (no Java/Android SDK toolchain is available in this dev environment to verify a hand-written one). A full implementation plan mirroring the extension's architecture is ready to build from: see [`../docs/ANDROID_PLAN.md`](../docs/ANDROID_PLAN.md).
