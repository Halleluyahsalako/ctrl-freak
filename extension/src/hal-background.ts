// Service worker (Manifest V3 background). Phase 1 will add the actual
// Firestore realtime listener here so clips sync even when the popup is
// closed. Left as a stub until then.

console.log("Ctrl+Freak background service worker loaded");
