# extension/

Manifest V3 browser extension (Chrome, Brave, Edge natively; Firefox via `webextension-polyfill`), TypeScript + Preact.

Not scaffolded yet — this is Phase 1 work (see `../ARCHITECTURE.md §4`), since Phase 0 is foundations only (no UI).
